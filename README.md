# Task Tracker — Система управления задачами сотрудников

##  Описание
REST API для управления задачами и сотрудниками. Поддерживает CRUD-операции, анализ загруженности и рекомендации по распределению важных задач.

##  Основной функционал

### 1. **Управление сотрудниками**
- Добавление, редактирование, удаление и просмотр сотрудников
- Поддержка ключевых атрибутов:
  - ФИО
  - Должность
  - Отдел
  - Email (уникальный)
  - Дата приёма на работу
  - Статус активности (`is_active`)
  - Максимальная нагрузка (`workload_capacity` — сколько активных задач может выполнять сотрудник)

### 2. **Управление задачами**
- Полный CRUD для задач
- Поддержка:
  - Названия и срока выполнения
  - Статуса (`not_started`, `in_progress`, `done`)
  - Назначения исполнителя (связь с сотрудником)
  - Иерархии задач (родительская → дочерняя)

### 3. **Аналитика и рекомендации**
- **Занятые сотрудники**  
  Эндпоинт возвращает список сотрудников, отсортированный по количеству **активных задач** (статус ≠ `done`).

- **Важные задачи**  
  Автоматически выявляет задачи, которые:
  - **Не начаты** (`not_started`),
  - Но от которых **зависят другие задачи, уже в работе** (`in_progress`).

  Для каждой такой задачи система:
  - Находит наименее загруженных сотрудников (с учётом `workload_capacity`)
  - Предлагает назначить задачу:
    - Исполнителю родительской задачи (если он не перегружен),
    - Или любому подходящему сотруднику (нагрузка ≤ min + 2 активных задач)

---

## 🛠️ Технологии
- Python 3.11
- Django 5.0 + DRF
- PostgreSQL
- Docker
- drf-spectacular (Swagger/ReDoc)

## 🚀 Установка и запуск

1. Клонируйте репозиторий:
   ```bash
   git clone <ваш-репозиторий>
   cd task_tracker